#!/usr/bin/env groovy
@Library('sim2')_

//log.info 'Starting'
//log.warning 'Nothing to do!'


node{
	log.info 'Starting Pipeline'
			stage ('Checkout'){
				    log.info 'Starting'
   					gitCheckOut()
 					log.info 'Finished Check Out'
   
				}
 
			stage('BUILD'){
					log.info 'Starting Build Maven'
    					buildMaven()
    							
					}
					stage('CLEAN'){
					log.info 'Starting Build Clean'
    					build()
    							
					}
					
 				if (env.BRANCH_NAME == 'master'){

						log.info 'Selected Master Branch'
					
			stage('TEST'){
    
    					buildTest()
  						}
   					 }
  			}